import {
  container,
  description,
  cardTitle,
  blackColor,
  whiteColor,
  grayColor,
  hexToRgb
} from "assets/jss/nextjs-material-kit-pro.js";

const logoStyle = theme => ({
  imageIcon: {
    height: "100%"
  },
  iconRoot: {
    textAlign: "center"
  }
});

export default logoStyle;
